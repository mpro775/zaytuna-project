# كيفية تفعيل وضع الموكاب (Mock Mode)

> **ملاحظة مهمة**: وضع الموكاب **مفعّل افتراضياً** في التطبيق. لا حاجة لتفعيله يدوياً إلا إذا تم تعطيله مسبقاً.

---

هناك **طريقتان** لتفعيل/تعطيل وضع البيانات الوهمية:

---

## 🚀 الطريقة الأولى: من الإعدادات (الأسهل والأسرع)

### للتفعيل (إذا كان معطلاً):

1. **سجل الدخول** إلى التطبيق بأي حساب:
   - `admin` / `admin123`
   - `manager` / `manager123`
   - `cashier` / `cashier123`

2. **انتقل إلى صفحة الإعدادات**:
   - من القائمة الجانبية، اضغط على **"الإعدادات"**
   - أو اكتب `/settings` في شريط العنوان

3. **افتح تبويب "إعدادات النظام"**:
   - في صفحة الإعدادات، اضغط على التبويب الثاني **"إعدادات النظام"**

4. **ابحث عن قسم "وضع البيانات الوهمية"**:
   - سترى مفتاح تبديل (Switch) بعنوان **"تفعيل وضع البيانات الوهمية"**

5. **فعّل المفتاح** (إذا كان معطلاً):
   - اضغط على المفتاح لتفعيله
   - سيظهر تنبيه يطلب التأكيد

6. **أكد التغيير**:
   - اضغط على زر **"تأكيد وإعادة التحميل"**
   - سيتم إعادة تحميل الصفحة تلقائياً

7. **تحقق من التفعيل**:
   - بعد إعادة التحميل، سترى رسالة في أعلى الصفحة تشير إلى أن وضع Mock مفعّل
   - أو رسالة في Console المتصفح: `⚠️ Mock Data Mode is ENABLED`

> **ملاحظة**: بما أن الوضع مفعّل افتراضياً، ستجد المفتاح مفعّلاً عند أول فتح للتطبيق.

---

## ⚙️ الطريقة الثانية: من ملف البيئة (.env)

### للتفعيل (إذا أردت التأكيد):

1. **أنشئ ملف `.env`** في مجلد `frontend-customer` (إذا لم يكن موجوداً):
   ```
   frontend-customer/
   └── .env
   ```

2. **أضف المتغير التالي** إلى الملف (اختياري - الوضع مفعّل افتراضياً):
   ```env
   VITE_USE_MOCK_DATA=true
   ```

3. **أعد تشغيل خادم التطوير**:
   - أوقف الخادم (Ctrl+C)
   - أعد تشغيله:
     ```bash
     npm run dev
     # أو
     yarn dev
     ```

4. **تحقق من التفعيل**:
   - افتح Console المتصفح
   - يجب أن ترى: `⚠️ Mock Data Mode is ENABLED`

### لتعطيل الوضع:

إذا أردت تعطيل وضع Mock من ملف `.env`:
```env
VITE_USE_MOCK_DATA=false
```

---

## 🔍 كيفية التحقق من أن وضع Mock مفعّل

### 1. من Console المتصفح:
افتح Developer Tools (F12) وابحث عن:
```
⚠️ Mock Data Mode is ENABLED
```
> **ملاحظة**: بما أن الوضع مفعّل افتراضياً، يجب أن ترى هذه الرسالة عند فتح التطبيق لأول مرة.

### 2. من واجهة التطبيق:
- في صفحة الإعدادات > إعدادات النظام
- قسم "وضع البيانات الوهمية"
- يجب أن يكون المفتاح في وضع **"مفعّل"** (أزرق/أخضر)
- **افتراضياً**: المفتاح سيكون مفعّلاً عند أول فتح

### 3. من localStorage:
افتح Console المتصفح واكتب:
```javascript
localStorage.getItem('mockDataEnabled')
// إذا كان مفعّلاً: "true"
// إذا لم يتم تعيينه مسبقاً: null (لكن الوضع مفعّل افتراضياً)
```

---

## ❌ كيفية إلغاء تفعيل وضع Mock

### من الإعدادات:
1. اذهب إلى **الإعدادات** > **إعدادات النظام**
2. في قسم **"وضع البيانات الوهمية"**
3. **ألغِ تفعيل** المفتاح
4. اضغط **"تأكيد وإعادة التحميل"**

### من ملف .env:
1. افتح ملف `.env`
2. غيّر القيمة إلى:
   ```env
   VITE_USE_MOCK_DATA=false
   ```
3. أو احذف السطر تماماً
4. أعد تشغيل خادم التطوير

---

## 📝 ملاحظات مهمة

### ⚠️ تحذيرات:
- عند تفعيل/إلغاء تفعيل وضع Mock، **سيتم إعادة تحميل الصفحة تلقائياً**
- جميع التغييرات في البيانات الوهمية تُحفظ في **localStorage** فقط
- البيانات الوهمية **لا تؤثر على النظام الحقيقي**

### ✅ فوائد وضع Mock:
- تجربة النظام بدون اتصال بخادم حقيقي
- اختبار الوظائف بسرعة
- تطوير الواجهة بدون انتظار Backend
- بيانات جاهزة للتجريب

### 🔄 إعادة تعيين البيانات:
إذا أردت إعادة تعيين جميع البيانات الوهمية إلى الحالة الافتراضية:
```javascript
// في Console المتصفح
localStorage.clear();
location.reload();
```

---

## 🎯 سيناريو سريع

**الهدف**: التحقق من أن وضع Mock مفعّل (مفعّل افتراضياً)

1. افتح التطبيق
2. سجل الدخول: `admin` / `admin123`
3. اذهب إلى: `/settings`
4. اضغط على تبويب: **"إعدادات النظام"**
5. تحقق من قسم **"وضع البيانات الوهمية"**
6. ✅ يجب أن يكون المفتاح **مفعّل** افتراضياً!

**إذا كان معطلاً**:
- فعّل المفتاح
- اضغط: **"تأكيد وإعادة التحميل"**

---

## 🆘 حل المشاكل

### المشكلة: المفتاح لا يظهر في الإعدادات
**الحل**:
- تأكد من أنك في تبويب **"إعدادات النظام"** وليس "إعدادات الشركة"
- تأكد من تسجيل الدخول

### المشكلة: التفعيل لا يعمل
**الحل**:
1. افتح Console المتصفح (F12)
2. تحقق من وجود أخطاء
3. جرب إعادة تحميل الصفحة يدوياً (F5)
4. تحقق من localStorage:
   ```javascript
   localStorage.getItem('mockDataEnabled')
   ```

### المشكلة: البيانات لا تظهر بعد التفعيل
**الحل**:
1. تأكد من أن وضع Mock مفعّل فعلاً
2. تحقق من Console للأخطاء
3. تأكد من وجود البيانات في ملفات JSON:
   - `src/mocks/data/products.json`
   - `src/mocks/data/customers.json`
   - إلخ...

---

## 📚 موارد إضافية

- **دليل كامل للصفحات**: `src/mocks/GUIDE.md`
- **README نظام الموكاب**: `src/mocks/README.md`
- **إعدادات Mock**: `src/config/mock.config.ts`

---

**آخر تحديث**: 2024

