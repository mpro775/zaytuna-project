# 🔄 وحدة المرتجعات (Returns Module)

## نظرة عامة

وحدة المرتجعات مسؤولة عن إدارة مرتجعات المنتجات وإشعارات الدائن في النظام. توفر هذه الوحدة نظام مرتجعات شامل مع دعم الاسترداد النقدي وإشعارات الدائن.

### الميزات الرئيسية

- **إدارة المرتجعات**: إنشاء وتحديث وإلغاء المرتجعات
- **إشعارات الدائن**: إنشاء إشعارات دائن للمرتجعات
- **البحث والفلترة**: البحث حسب الفاتورة، العميل، الحالة
- **الإحصائيات**: إحصائيات شاملة عن المرتجعات

---

## API Endpoints

### POST `/returns` - إنشاء مرتجع جديد
**الصلاحيات**: `returns.create`

### GET `/returns` - الحصول على المرتجعات
**الصلاحيات**: `returns.read`
- Query: `salesInvoiceId`, `customerId`, `status`, `refundStatus`, `limit`

### GET `/returns/:id` - الحصول على مرتجع بالمعرف
**الصلاحيات**: `returns.read`

### PATCH `/returns/:id` - تحديث مرتجع
**الصلاحيات**: `returns.update`

### DELETE `/returns/:id/cancel` - إلغاء مرتجع
**الصلاحيات**: `returns.update`

### POST `/returns/:id/credit-notes` - إنشاء إشعار دائن
**الصلاحيات**: `returns.update`

### GET `/returns/stats/overview` - إحصائيات المرتجعات
**الصلاحيات**: `returns.read`
- Query: `startDate`, `endDate`

### GET `/returns/sales-invoices/:salesInvoiceId/returns` - مرتجعات فاتورة
**الصلاحيات**: `returns.read`

### GET `/returns/customers/:customerId/returns` - مرتجعات العميل
**الصلاحيات**: `returns.read`

---

## DTOs

### CreateReturnDto
- `returnNumber`: string (اختياري)
- `salesInvoiceId`: string (مطلوب، UUID)
- `customerId`: string (اختياري، UUID)
- `warehouseId`: string (مطلوب، UUID)
- `currencyId`: string (مطلوب، UUID)
- `lines`: ReturnLineDto[] (مطلوب)
- `reason`: string (مطلوب)
- `notes`: string (اختياري)

### ReturnLineDto
- `productVariantId`: string (مطلوب، UUID)
- `quantity`: number (مطلوب)
- `unitPrice`: number (اختياري)
- `reason`: string (اختياري)

### CreateCreditNoteDto
- `amount`: number (مطلوب)
- `expiryDate`: Date (اختياري)
- `notes`: string (اختياري)

---

## العلاقات

- **Sales Module**: المرتجعات مرتبطة بفواتير المبيعات
- **Customer Module**: المرتجعات مرتبطة بالعملاء
- **Warehouse Module**: المرتجعات مرتبطة بالمخازن
- **Inventory Module**: المرتجعات تزيد من المخزون

---

**📅 تاريخ آخر تحديث**: ديسمبر 2025

