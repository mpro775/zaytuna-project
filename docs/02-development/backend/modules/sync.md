# 🔄 وحدة المزامنة (Sync Module)

## نظرة عامة

وحدة المزامنة مسؤولة عن مزامنة البيانات بين الخادم والأجهزة في وضع offline. توفر هذه الوحدة نظام مزامنة شامل مع حل التعارضات ودعم العمل offline.

### الميزات الرئيسية

- **مزامنة ثنائية الاتجاه**: رفع وتحميل البيانات
- **حل التعارضات**: حل تلقائي ويدوي للتعارضات
- **دعم Offline**: جلسات عمل offline
- **تتبع الحالة**: تتبع حالة دفعات المزامنة

---

## API Endpoints

### المزامنة

#### POST `/sync/upload` - رفع دفعة مزامنة
**الصلاحيات**: `sync.upload`

#### GET `/sync/download` - تحميل بيانات المزامنة
**الصلاحيات**: `sync.download`

#### POST `/sync/bidirectional` - مزامنة ثنائية الاتجاه
**الصلاحيات**: `sync.bidirectional`

### إدارة الدفعات

#### GET `/sync/batch/:batchId` - حالة دفعة
**الصلاحيات**: `sync.read`

#### PUT `/sync/batch/:batchId/conflict/:conflictId` - حل تعارض
**الصلاحيات**: `sync.resolve`

#### POST `/sync/batch/:batchId/retry` - إعادة محاولة
**الصلاحيات**: `sync.retry`

### دعم Offline

#### POST `/sync/offline/session` - إنشاء جلسة offline
**الصلاحيات**: `sync.offline`

#### GET `/sync/offline/package/:sessionId` - حزمة بيانات offline
**الصلاحيات**: `sync.offline`

#### POST `/sync/offline/changes/:sessionId` - حفظ تغييرات offline
**الصلاحيات**: `sync.offline`

### الإحصائيات

#### GET `/sync/stats` - إحصائيات المزامنة
**الصلاحيات**: `sync.read`

#### GET `/sync/health` - صحة المزامنة
**الصلاحيات**: لا شيء (Public)

---

## العلاقات

- **جميع الوحدات**: جميع الوحدات تدعم المزامنة
- **Audit Module**: تسجيل عمليات المزامنة
- **Branch Module**: المزامنة حسب الفروع

---

**📅 تاريخ آخر تحديث**: ديسمبر 2025

