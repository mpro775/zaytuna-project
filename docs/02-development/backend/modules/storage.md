# 📁 وحدة التخزين (Storage Module)

## نظرة عامة

وحدة التخزين مسؤولة عن إدارة الملفات والتخزين في النظام. توفر هذه الوحدة نظام تخزين شامل مع دعم التخزين المحلي والسحابي، تحسين الصور، وإدارة الوصول.

### الميزات الرئيسية

- **رفع الملفات**: رفع ملفات فردية ومتعددة
- **تحسين الصور**: تحسين وتحويل تنسيقات الصور
- **إدارة الوصول**: رموز وصول مؤقتة وروابط عامة
- **الصور المصغرة**: إنشاء صور مصغرة تلقائياً

---

## API Endpoints

### رفع الملفات

#### POST `/storage/upload` - رفع ملف واحد
**الصلاحيات**: `storage.upload`

#### POST `/storage/upload/multiple` - رفع ملفات متعددة
**الصلاحيات**: `storage.upload`

### تحميل وعرض الملفات

#### GET `/storage/files/:fileId/download` - تحميل ملف
**الصلاحيات**: `storage.download`

#### GET `/storage/files/:fileId/view` - عرض ملف
**الصلاحيات**: `storage.view`

#### GET `/storage/files/:fileId/thumbnail` - عرض صورة مصغرة
**الصلاحيات**: `storage.view`

### إدارة الملفات

#### GET `/storage/files/:fileId` - معلومات ملف
**الصلاحيات**: `storage.read`

#### GET `/storage/files` - البحث في الملفات
**الصلاحيات**: `storage.read`

#### DELETE `/storage/files/:fileId` - حذف ملف
**الصلاحيات**: `storage.delete`

### إدارة الوصول

#### POST `/storage/files/:fileId/access-token` - إنشاء رمز وصول
**الصلاحيات**: `storage.share`

#### POST `/storage/files/:fileId/public-link` - إنشاء رابط عام
**الصلاحيات**: `storage.share`

### تحسين الصور

#### POST `/storage/files/:fileId/optimize` - تحسين صورة
**الصلاحيات**: `storage.edit`

#### POST `/storage/files/:fileId/thumbnails` - إنشاء صور مصغرة
**الصلاحيات**: `storage.edit`

#### POST `/storage/files/:fileId/convert` - تحويل تنسيق
**الصلاحيات**: `storage.edit`

---

## العلاقات

- **جميع الوحدات**: جميع الوحدات تستخدم Storage Module لتخزين الملفات
- **User Module**: تتبع الملفات المرفوعة
- **Branch Module**: تتبع الملفات حسب الفرع

---

**📅 تاريخ آخر تحديث**: ديسمبر 2025

