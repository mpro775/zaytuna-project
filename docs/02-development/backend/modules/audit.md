# 📝 وحدة التدقيق (Audit Module)

## نظرة عامة

وحدة التدقيق مسؤولة عن تسجيل ومراقبة جميع العمليات في النظام. توفر هذه الوحدة نظام تدقيق شامل مع تتبع التغييرات وإحصائيات الأمان.

### الميزات الرئيسية

- **تسجيل العمليات**: تسجيل جميع العمليات في النظام
- **البحث المتقدم**: بحث متقدم في سجلات التدقيق
- **التقارير**: تقارير أمان وامتثال
- **التصدير**: تصدير سجلات التدقيق

---

## API Endpoints

### البحث والاستعلام

#### GET `/audit/logs` - البحث في سجلات التدقيق
**الصلاحيات**: `audit.read`
- Query: `userId`, `action`, `entity`, `entityId`, `branchId`, `warehouseId`, `startDate`, `endDate`, `success`, `severity`, `category`, `referenceType`, `referenceId`, `module`, `searchText`, `limit`, `offset`

#### GET `/audit/logs/:id` - الحصول على سجل بالمعرف
**الصلاحيات**: `audit.read`

### الإحصائيات

#### GET `/audit/stats` - إحصائيات التدقيق
**الصلاحيات**: `audit.read`
- Query: `startDate`, `endDate`, `branchId`

#### GET `/audit/stats/daily` - إحصائيات يومية
**الصلاحيات**: `audit.read`

#### GET `/audit/stats/weekly` - إحصائيات أسبوعية
**الصلاحيات**: `audit.read`

#### GET `/audit/stats/monthly` - إحصائيات شهرية
**الصلاحيات**: `audit.read`

### التتبع

#### GET `/audit/trail/entity/:entity/:entityId` - تتبع كيان
**الصلاحيات**: `audit.read`

#### GET `/audit/trail/user/:userId` - تتبع مستخدم
**الصلاحيات**: `audit.read`

### التقارير

#### GET `/audit/reports/errors` - تقرير الأخطاء
**الصلاحيات**: `audit.read`

#### GET `/audit/reports/security` - تقرير الأمان
**الصلاحيات**: `audit.read`

#### GET `/audit/reports/activity` - تقرير النشاط
**الصلاحيات**: `audit.read`

#### GET `/audit/reports/compliance` - تقرير الامتثال
**الصلاحيات**: `audit.compliance`

#### GET `/audit/reports/changes` - تقرير التغييرات
**الصلاحيات**: `audit.read`

### التصدير

#### GET `/audit/export/excel` - تصدير إلى Excel
**الصلاحيات**: `audit.export`

#### GET `/audit/export/json` - تصدير إلى JSON
**الصلاحيات**: `audit.export`

### الإدارة

#### GET `/audit/cleanup/:days` - تنظيف السجلات القديمة
**الصلاحيات**: `audit.admin`

---

## العلاقات

- **جميع الوحدات**: جميع الوحدات تستخدم Audit Module لتسجيل العمليات
- **User Module**: تتبع نشاط المستخدمين
- **Branch Module**: تتبع نشاط الفروع

---

**📅 تاريخ آخر تحديث**: ديسمبر 2025

