# 🔄 وحدة متغيرات المنتجات (Product Variant Module)

## نظرة عامة

وحدة متغيرات المنتجات مسؤولة عن إدارة متغيرات المنتجات (مثل الألوان، الأحجام، إلخ). كل منتج يمكن أن يحتوي على عدة متغيرات مع أسعار وخصائص مختلفة.

### الميزات الرئيسية

- **إدارة المتغيرات**: إنشاء وتحديث وحذف متغيرات المنتجات
- **البحث**: البحث بالباركود أو SKU
- **الفلترة**: البحث حسب المنتج

### الاعتماديات

- `PrismaService`: للوصول إلى قاعدة البيانات
- `ProductModule`: للتحقق من صحة المنتجات

---

## API Endpoints

### POST `/product-variants`
إنشاء متغير منتج جديد - **الصلاحيات**: `products.create`

### GET `/product-variants`
الحصول على جميع المتغيرات - **الصلاحيات**: `products.read`
- Query: `productId`

### GET `/product-variants/lookup/:identifier`
البحث عن متغير بالباركود أو SKU - **الصلاحيات**: `products.read`

### GET `/product-variants/:id`
الحصول على متغير بالمعرف - **الصلاحيات**: `products.read`

### PATCH `/product-variants/:id`
تحديث متغير - **الصلاحيات**: `products.update`

### DELETE `/product-variants/:id`
حذف متغير - **الصلاحيات**: `products.delete`

---

## DTOs

### CreateProductVariantDto
- `productId`: string (مطلوب، UUID)
- `name`: string (مطلوب)
- `sku`: string (اختياري، فريد)
- `barcode`: string (اختياري، فريد)
- `price`: number (اختياري، ≥ 0)
- `costPrice`: number (اختياري، ≥ 0)
- `weight`: number (اختياري، ≥ 0)
- `dimensions`: object (اختياري)
- `attributes`: object (اختياري)
- `imageUrl`: string (اختياري)
- `isActive`: boolean (افتراضي: true)

---

## العلاقات

- **Product Module**: المتغيرات تنتمي إلى منتجات
- **Inventory Module**: المتغيرات مرتبطة بعناصر المخزون
- **Sales Module**: المتغيرات تُستخدم في فواتير المبيعات

---

**📅 تاريخ آخر تحديث**: ديسمبر 2025

