# ⚡ وحدة الأداء (Performance Module)

## نظرة عامة

وحدة الأداء مسؤولة عن تحسين أداء النظام. توفر هذه الوحدة تحسين الاستعلامات، تحسين الكاش، واختبارات الحمل.

### الميزات الرئيسية

- **تحسين الاستعلامات**: تحليل وتحسين استعلامات قاعدة البيانات
- **تحسين الكاش**: تحسين استخدام الكاش
- **اختبارات الحمل**: اختبارات أداء النظام

---

## API Endpoints

#### GET `/performance/report` - تقرير الأداء
**الصلاحيات**: `performance:read`

#### GET `/performance/cache/stats` - إحصائيات الكاش
**الصلاحيات**: `performance:read`

#### POST `/performance/cache/optimize` - تحسين الكاش
**الصلاحيات**: `performance:admin`

#### POST `/performance/load-test/run` - تشغيل اختبار حمل
**الصلاحيات**: `performance:admin`

#### GET `/performance/queries/sales-optimized` - مبيعات محسنة
**الصلاحيات**: `performance:read`

---

## العلاقات

- **Database**: تحسين استعلامات قاعدة البيانات
- **Cache**: تحسين استخدام الكاش
- **جميع الوحدات**: تحسين أداء جميع الوحدات

---

**📅 تاريخ آخر تحديث**: ديسمبر 2025

