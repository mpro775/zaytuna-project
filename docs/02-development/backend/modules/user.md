# 👤 وحدة المستخدمين (User Module)

## نظرة عامة

وحدة المستخدمين مسؤولة عن إدارة حسابات المستخدمين في النظام. توفر هذه الوحدة وظائف شاملة لإنشاء وتحديث وحذف المستخدمين، بالإضافة إلى إدارة حالاتهم وكلمات مرورهم.

### الميزات الرئيسية

- **إدارة المستخدمين**: إنشاء وتحديث وحذف المستخدمين
- **إدارة الحالة**: تفعيل وإلغاء تفعيل المستخدمين
- **إدارة كلمات المرور**: تغيير كلمات المرور
- **الفلترة والبحث**: البحث عن المستخدمين حسب الفرع أو الدور
- **الإحصائيات**: إحصائيات شاملة عن المستخدمين

### الاعتماديات

- `PrismaService`: للوصول إلى قاعدة البيانات
- `AuthModule`: للتحقق من الصلاحيات
- `BranchModule`: للتحقق من صحة الفروع

---

## API Endpoints

### إدارة المستخدمين

#### POST `/users`
إنشاء مستخدم جديد

**الصلاحيات المطلوبة**: `users.create`

**Request Body**:
```json
{
  "username": "newuser",
  "email": "user@example.com",
  "password": "password123",
  "phone": "+967712345678",
  "roleId": "clx1111111111",
  "branchId": "clx2222222222",
  "isActive": true
}
```

**Response** (201 Created):
```json
{
  "id": "clx1234567890",
  "username": "newuser",
  "email": "user@example.com",
  "phone": "+967712345678",
  "isActive": true,
  "lastLoginAt": null,
  "branchId": "clx2222222222",
  "roleId": "clx1111111111",
  "twoFactorEnabled": false,
  "biometricEnabled": false,
  "role": {
    "id": "clx1111111111",
    "name": "cashier",
    "description": "كاشير",
    "permissions": ["sales.create", "sales.read"]
  },
  "branch": {
    "id": "clx2222222222",
    "name": "الفرع الرئيسي"
  },
  "createdAt": "2025-12-01T10:00:00.000Z",
  "updatedAt": "2025-12-01T10:00:00.000Z"
}
```

**الأخطاء المحتملة**:
- `409 Conflict`: اسم المستخدم موجود بالفعل
- `409 Conflict`: البريد الإلكتروني موجود بالفعل
- `404 Not Found`: الدور غير موجود
- `404 Not Found`: الفرع غير موجود

---

#### GET `/users`
الحصول على جميع المستخدمين

**الصلاحيات المطلوبة**: `users.read`

**Query Parameters**:
- `branchId` (optional): فلترة حسب الفرع
- `roleId` (optional): فلترة حسب الدور
- `isActive` (optional): فلترة حسب الحالة (true/false)

**Response** (200 OK):
```json
[
  {
    "id": "clx1234567890",
    "username": "admin",
    "email": "admin@example.com",
    "phone": "+967712345678",
    "isActive": true,
    "lastLoginAt": "2025-12-01T09:00:00.000Z",
    "branchId": "clx2222222222",
    "roleId": "clx1111111111",
    "role": {
      "id": "clx1111111111",
      "name": "admin",
      "description": "مدير النظام"
    },
    "branch": {
      "id": "clx2222222222",
      "name": "الفرع الرئيسي"
    },
    "createdAt": "2025-01-01T00:00:00.000Z",
    "updatedAt": "2025-12-01T10:00:00.000Z"
  }
]
```

---

#### GET `/users/:id`
الحصول على مستخدم بالمعرف

**الصلاحيات المطلوبة**: `users.read`

**Response** (200 OK):
```json
{
  "id": "clx1234567890",
  "username": "admin",
  "email": "admin@example.com",
  "phone": "+967712345678",
  "isActive": true,
  "lastLoginAt": "2025-12-01T09:00:00.000Z",
  "branchId": "clx2222222222",
  "roleId": "clx1111111111",
  "twoFactorEnabled": false,
  "biometricEnabled": false,
  "role": {
    "id": "clx1111111111",
    "name": "admin",
    "description": "مدير النظام",
    "permissions": ["*"]
  },
  "branch": {
    "id": "clx2222222222",
    "name": "الفرع الرئيسي",
    "code": "BR001"
  },
  "createdAt": "2025-01-01T00:00:00.000Z",
  "updatedAt": "2025-12-01T10:00:00.000Z"
}
```

**الأخطاء المحتملة**:
- `404 Not Found`: المستخدم غير موجود

---

#### PATCH `/users/:id`
تحديث مستخدم

**الصلاحيات المطلوبة**: `users.update`

**Request Body**:
```json
{
  "email": "updated@example.com",
  "phone": "+967798765432",
  "branchId": "clx3333333333",
  "roleId": "clx4444444444"
}
```

**Response** (200 OK):
```json
{
  "id": "clx1234567890",
  "username": "admin",
  "email": "updated@example.com",
  "phone": "+967798765432",
  "isActive": true,
  "branchId": "clx3333333333",
  "roleId": "clx4444444444",
  "updatedAt": "2025-12-01T11:00:00.000Z"
}
```

**الأخطاء المحتملة**:
- `404 Not Found`: المستخدم غير موجود
- `409 Conflict`: البريد الإلكتروني موجود بالفعل
- `404 Not Found`: الدور غير موجود
- `404 Not Found`: الفرع غير موجود

---

#### DELETE `/users/:id`
حذف مستخدم

**الصلاحيات المطلوبة**: `users.delete`

**Response** (200 OK):
```json
{
  "message": "تم حذف المستخدم بنجاح"
}
```

**الأخطاء المحتملة**:
- `404 Not Found`: المستخدم غير موجود
- `400 Bad Request`: لا يمكن حذف المستخدم لأنه مرتبط ببيانات أخرى

---

#### PATCH `/users/:id/password`
تغيير كلمة مرور المستخدم

**الصلاحيات المطلوبة**: `users.update`

**Request Body**:
```json
{
  "newPassword": "newpassword123"
}
```

**Response** (200 OK):
```json
{
  "message": "تم تغيير كلمة المرور بنجاح"
}
```

**الأخطاء المحتملة**:
- `404 Not Found`: المستخدم غير موجود
- `400 Bad Request`: كلمة المرور غير صحيحة (قصيرة جداً)

---

#### PUT `/users/:id/toggle-status`
تبديل حالة المستخدم (تفعيل/إلغاء تفعيل)

**الصلاحيات المطلوبة**: `users.update`

**Response** (200 OK):
```json
{
  "id": "clx1234567890",
  "username": "admin",
  "isActive": false,
  "updatedAt": "2025-12-01T11:00:00.000Z"
}
```

**الأخطاء المحتملة**:
- `404 Not Found`: المستخدم غير موجود

---

#### GET `/users/stats/overview`
الحصول على إحصائيات المستخدمين

**الصلاحيات المطلوبة**: `users.read`

**Response** (200 OK):
```json
{
  "totalUsers": 50,
  "activeUsers": 45,
  "inactiveUsers": 5,
  "usersByRole": {
    "admin": 5,
    "manager": 10,
    "cashier": 35
  },
  "usersByBranch": {
    "الفرع الرئيسي": 30,
    "الفرع الثاني": 20
  },
  "recentLogins": 40,
  "neverLoggedIn": 10
}
```

---

## DTOs (Data Transfer Objects)

### CreateUserDto
```typescript
{
  username: string;      // اسم المستخدم (مطلوب، فريد)
  email: string;         // البريد الإلكتروني (مطلوب، فريد، صيغة صحيحة)
  password: string;      // كلمة المرور (مطلوب، حد أدنى 6 أحرف)
  phone?: string;        // رقم الهاتف (اختياري)
  roleId: string;        // معرف الدور (مطلوب، UUID)
  branchId?: string;     // معرف الفرع (اختياري، UUID)
  isActive?: boolean;    // حالة المستخدم (افتراضي: true)
}
```

**قواعد التحقق**:
- `username`: مطلوب، نص، فريد
- `email`: مطلوب، بريد إلكتروني صحيح، فريد
- `password`: مطلوب، نص، حد أدنى 6 أحرف
- `phone`: اختياري، نص
- `roleId`: مطلوب، UUID صحيح
- `branchId`: اختياري، UUID صحيح
- `isActive`: اختياري، قيمة منطقية

---

### UpdateUserDto
```typescript
{
  email?: string;         // البريد الإلكتروني
  phone?: string;        // رقم الهاتف
  branchId?: string;     // معرف الفرع
  roleId?: string;       // معرف الدور
  isActive?: boolean;    // حالة المستخدم
}
```

**قواعد التحقق**:
- جميع الحقول اختيارية
- `email`: إذا تم توفيره، يجب أن يكون بريد إلكتروني صحيح وفريد
- `roleId`: إذا تم توفيره، يجب أن يكون UUID صحيح
- `branchId`: إذا تم توفيره، يجب أن يكون UUID صحيح

---

### ChangeUserPasswordDto
```typescript
{
  newPassword: string;    // كلمة المرور الجديدة (مطلوب، حد أدنى 6 أحرف)
}
```

**قواعد التحقق**:
- `newPassword`: مطلوب، نص، حد أدنى 6 أحرف

---

## الخدمات (Services)

### UserService

#### `create(createUserDto: CreateUserDto): Promise<User>`
إنشاء مستخدم جديد

**المعاملات**:
- `createUserDto`: بيانات إنشاء المستخدم

**القيمة المرجعة**: المستخدم المُنشأ (بدون passwordHash)

**الاستثناءات**:
- `ConflictException`: إذا كان اسم المستخدم أو البريد موجوداً
- `NotFoundException`: إذا كان الدور أو الفرع غير موجود

---

#### `findAll(options?: UserFilterOptions): Promise<User[]>`
الحصول على جميع المستخدمين مع الفلترة

**المعاملات**:
- `options`: خيارات الفلترة (branchId, roleId, isActive)

**القيمة المرجعة**: مصفوفة من المستخدمين

---

#### `findOne(id: string): Promise<User>`
الحصول على مستخدم بالمعرف

**المعاملات**:
- `id`: معرف المستخدم

**القيمة المرجعة**: المستخدم

**الاستثناءات**:
- `NotFoundException`: إذا كان المستخدم غير موجود

---

#### `update(id: string, updateUserDto: UpdateUserDto): Promise<User>`
تحديث مستخدم

**المعاملات**:
- `id`: معرف المستخدم
- `updateUserDto`: بيانات التحديث

**القيمة المرجعة**: المستخدم المحدث

**الاستثناءات**:
- `NotFoundException`: إذا كان المستخدم غير موجود
- `ConflictException`: إذا كان البريد الإلكتروني موجوداً
- `NotFoundException`: إذا كان الدور أو الفرع غير موجود

---

#### `remove(id: string): Promise<void>`
حذف مستخدم

**المعاملات**:
- `id`: معرف المستخدم

**الاستثناءات**:
- `NotFoundException`: إذا كان المستخدم غير موجود
- `BadRequestException`: إذا كان المستخدم مرتبط ببيانات أخرى

---

#### `changePassword(id: string, changePasswordDto: ChangeUserPasswordDto): Promise<void>`
تغيير كلمة مرور المستخدم

**المعاملات**:
- `id`: معرف المستخدم
- `changePasswordDto`: بيانات تغيير كلمة المرور

**الاستثناءات**:
- `NotFoundException`: إذا كان المستخدم غير موجود
- `BadRequestException`: إذا كانت كلمة المرور غير صحيحة

---

#### `toggleUserStatus(id: string): Promise<User>`
تبديل حالة المستخدم (تفعيل/إلغاء تفعيل)

**المعاملات**:
- `id`: معرف المستخدم

**القيمة المرجعة**: المستخدم مع الحالة المحدثة

**الاستثناءات**:
- `NotFoundException`: إذا كان المستخدم غير موجود

---

#### `getUserStats(): Promise<UserStats>`
الحصول على إحصائيات المستخدمين

**القيمة المرجعة**: إحصائيات المستخدمين

---

## العلاقات (Relationships)

### الوحدات المرتبطة

- **Auth Module**: تستخدم وحدة User للمصادقة وإدارة الجلسات
- **Branch Module**: المستخدمون مرتبطون بالفروع
- **Role Module**: المستخدمون لديهم أدوار محددة
- **Audit Module**: جميع عمليات المستخدمين تُسجل في سجلات التدقيق

### التبعيات

- `PrismaService`: للوصول إلى جدول `users`
- `AuthModule`: للتحقق من الصلاحيات
- `BranchModule`: للتحقق من صحة الفروع

### الجداول المرتبطة

- `users`: جدول المستخدمين الرئيسي
- `roles`: جدول الأدوار
- `branches`: جدول الفروع
- `audit_logs`: سجلات التدقيق

---

## أمثلة الاستخدام

### مثال 1: إنشاء مستخدم جديد

```typescript
// Request
POST /users
Authorization: Bearer <access_token>
{
  "username": "cashier1",
  "email": "cashier1@example.com",
  "password": "password123",
  "phone": "+967712345678",
  "roleId": "clx1111111111",
  "branchId": "clx2222222222",
  "isActive": true
}

// Response
{
  "id": "clx1234567890",
  "username": "cashier1",
  "email": "cashier1@example.com",
  "phone": "+967712345678",
  "isActive": true,
  "role": {
    "id": "clx1111111111",
    "name": "cashier",
    "description": "كاشير"
  },
  "branch": {
    "id": "clx2222222222",
    "name": "الفرع الرئيسي"
  },
  "createdAt": "2025-12-01T10:00:00.000Z"
}
```

### مثال 2: البحث عن المستخدمين حسب الفرع

```typescript
// Request
GET /users?branchId=clx2222222222
Authorization: Bearer <access_token>

// Response
[
  {
    "id": "clx1234567890",
    "username": "cashier1",
    "email": "cashier1@example.com",
    "branch": {
      "id": "clx2222222222",
      "name": "الفرع الرئيسي"
    }
  }
]
```

### مثال 3: تبديل حالة المستخدم

```typescript
// Request
PUT /users/clx1234567890/toggle-status
Authorization: Bearer <access_token>

// Response
{
  "id": "clx1234567890",
  "username": "cashier1",
  "isActive": false,
  "updatedAt": "2025-12-01T11:00:00.000Z"
}
```

---

## الأخطاء الشائعة

### 409 Conflict - اسم المستخدم موجود بالفعل
**السبب**: محاولة إنشاء مستخدم باسم مستخدم موجود

**الحل**: 
- استخدام اسم مستخدم آخر
- أو تحديث المستخدم الموجود

---

### 409 Conflict - البريد الإلكتروني موجود بالفعل
**السبب**: محاولة إنشاء مستخدم ببريد إلكتروني موجود

**الحل**: 
- استخدام بريد إلكتروني آخر
- أو تحديث المستخدم الموجود

---

### 404 Not Found - الدور غير موجود
**السبب**: معرف الدور المرسل غير موجود في قاعدة البيانات

**الحل**: 
- التحقق من صحة معرف الدور
- إنشاء الدور أولاً إذا لم يكن موجوداً

---

### 404 Not Found - الفرع غير موجود
**السبب**: معرف الفرع المرسل غير موجود في قاعدة البيانات

**الحل**: 
- التحقق من صحة معرف الفرع
- إنشاء الفرع أولاً إذا لم يكن موجوداً

---

### 400 Bad Request - لا يمكن حذف المستخدم
**السبب**: المستخدم مرتبط ببيانات أخرى (فواتير، مدفوعات، إلخ)

**الحل**: 
- إلغاء تفعيل المستخدم بدلاً من حذفه
- أو حذف البيانات المرتبطة أولاً

---

**📅 تاريخ آخر تحديث**: ديسمبر 2025
**👨‍💻 المطور**: فريق تطوير BThwani
**📊 إصدار الوحدة**: v1.0.0

