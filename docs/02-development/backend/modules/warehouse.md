# 📦 وحدة المخازن (Warehouse Module)

## نظرة عامة

وحدة المخازن مسؤولة عن إدارة مخازن الشركة في النظام. توفر هذه الوحدة وظائف شاملة لإنشاء وتحديث وحذف المخازن، بالإضافة إلى إدارة علاقاتها مع الفروع والمستخدمين وعناصر المخزون.

### الميزات الرئيسية

- **إدارة المخازن**: إنشاء وتحديث وحذف المخازن
- **إدارة العلاقات**: ربط المخازن بالفروع والمستخدمين
- **نقل المخزون**: نقل المخزون بين المخازن
- **الإحصائيات**: إحصائيات شاملة عن المخازن
- **الفلترة**: البحث عن المخازن حسب الفرع

### الاعتماديات

- `PrismaService`: للوصول إلى قاعدة البيانات
- `CacheService`: لتخزين البيانات في الكاش
- `BranchModule`: للتحقق من صحة الفروع
- `UserModule`: للتحقق من صحة المستخدمين (المديرين)
- `InventoryModule`: لإدارة عناصر المخزون

---

## API Endpoints

### إدارة المخازن

#### POST `/warehouses`
إنشاء مخزن جديد

**الصلاحيات المطلوبة**: `branches.manage`

**Request Body**:
```json
{
  "name": "مخزن الفرع الرئيسي",
  "code": "WH001",
  "address": "صنعاء، شارع الزبيري",
  "phone": "+967712345678",
  "email": "warehouse1@example.com",
  "branchId": "clx3333333333",
  "managerId": "clx2222222222",
  "isActive": true
}
```

**Response** (201 Created):
```json
{
  "id": "clx8888888888",
  "name": "مخزن الفرع الرئيسي",
  "code": "WH001",
  "address": "صنعاء، شارع الزبيري",
  "phone": "+967712345678",
  "email": "warehouse1@example.com",
  "managerId": "clx2222222222",
  "branchId": "clx3333333333",
  "isActive": true,
  "manager": {
    "id": "clx2222222222",
    "username": "warehouse_manager",
    "email": "manager@example.com"
  },
  "branch": {
    "id": "clx3333333333",
    "name": "الفرع الرئيسي",
    "code": "BR001"
  },
  "stockItemCount": 0,
  "createdAt": "2025-12-01T10:00:00.000Z",
  "updatedAt": "2025-12-01T10:00:00.000Z"
}
```

**الأخطاء المحتملة**:
- `409 Conflict`: كود المخزن موجود بالفعل
- `404 Not Found`: الفرع غير موجود
- `404 Not Found`: المدير غير موجود

---

#### GET `/warehouses`
الحصول على جميع المخازن

**الصلاحيات المطلوبة**: `inventory.read`

**Query Parameters**:
- `branchId` (optional): فلترة حسب الفرع

**Response** (200 OK):
```json
[
  {
    "id": "clx8888888888",
    "name": "مخزن الفرع الرئيسي",
    "code": "WH001",
    "address": "صنعاء، شارع الزبيري",
    "phone": "+967712345678",
    "email": "warehouse1@example.com",
    "managerId": "clx2222222222",
    "branchId": "clx3333333333",
    "isActive": true,
    "branch": {
      "id": "clx3333333333",
      "name": "الفرع الرئيسي",
      "code": "BR001"
    },
    "stockItemCount": 150,
    "createdAt": "2025-01-01T00:00:00.000Z",
    "updatedAt": "2025-12-01T10:00:00.000Z"
  }
]
```

---

#### GET `/warehouses/stats`
الحصول على إحصائيات المخازن

**الصلاحيات المطلوبة**: `inventory.read`

**Response** (200 OK):
```json
{
  "totalWarehouses": 10,
  "activeWarehouses": 9,
  "inactiveWarehouses": 1,
  "totalStockItems": 5000,
  "warehousesByBranch": {
    "الفرع الرئيسي": 5,
    "الفرع الثاني": 5
  }
}
```

---

#### GET `/warehouses/:id`
الحصول على مخزن بالمعرف

**الصلاحيات المطلوبة**: `inventory.read`

**Response** (200 OK):
```json
{
  "id": "clx8888888888",
  "name": "مخزن الفرع الرئيسي",
  "code": "WH001",
  "address": "صنعاء، شارع الزبيري",
  "phone": "+967712345678",
  "email": "warehouse1@example.com",
  "managerId": "clx2222222222",
  "branchId": "clx3333333333",
  "isActive": true,
  "manager": {
    "id": "clx2222222222",
    "username": "warehouse_manager",
    "email": "manager@example.com"
  },
  "branch": {
    "id": "clx3333333333",
    "name": "الفرع الرئيسي",
    "code": "BR001"
  },
  "stockItemCount": 150,
  "createdAt": "2025-01-01T00:00:00.000Z",
  "updatedAt": "2025-12-01T10:00:00.000Z"
}
```

**الأخطاء المحتملة**:
- `404 Not Found`: المخزن غير موجود

---

#### GET `/warehouses/:id/stock`
الحصول على عناصر المخزون بالمخزن

**الصلاحيات المطلوبة**: `inventory.read`

**Response** (200 OK):
```json
[
  {
    "id": "clx9999999999",
    "warehouseId": "clx8888888888",
    "productVariantId": "clx1010101010",
    "quantity": 100.5,
    "minStock": 10,
    "maxStock": 1000,
    "productVariant": {
      "id": "clx1010101010",
      "name": "منتج - لون أحمر",
      "product": {
        "id": "clx2020202020",
        "name": "منتج"
      }
    }
  }
]
```

---

#### PATCH `/warehouses/:id`
تحديث مخزن

**الصلاحيات المطلوبة**: `branches.manage`

**Request Body**:
```json
{
  "name": "مخزن الفرع الرئيسي - محدث",
  "address": "صنعاء، شارع الزبيري - محدث",
  "phone": "+967798765432",
  "managerId": "clx6666666666"
}
```

**Response** (200 OK):
```json
{
  "id": "clx8888888888",
  "name": "مخزن الفرع الرئيسي - محدث",
  "code": "WH001",
  "address": "صنعاء، شارع الزبيري - محدث",
  "phone": "+967798765432",
  "managerId": "clx6666666666",
  "updatedAt": "2025-12-01T11:00:00.000Z"
}
```

**الأخطاء المحتملة**:
- `404 Not Found`: المخزن غير موجود
- `404 Not Found`: المدير غير موجود

---

#### POST `/warehouses/transfer-stock`
نقل المخزون بين المخازن

**الصلاحيات المطلوبة**: `inventory.transfer`

**Request Body**:
```json
{
  "fromWarehouseId": "clx8888888888",
  "toWarehouseId": "clx7777777777",
  "productVariantId": "clx1010101010",
  "quantity": 50.5,
  "notes": "نقل مخزون بين المخازن"
}
```

**Response** (200 OK):
```json
{
  "message": "تم نقل المخزون بنجاح",
  "transfer": {
    "id": "clx1111111111",
    "fromWarehouseId": "clx8888888888",
    "toWarehouseId": "clx7777777777",
    "productVariantId": "clx1010101010",
    "quantity": 50.5,
    "createdAt": "2025-12-01T11:00:00.000Z"
  }
}
```

**الأخطاء المحتملة**:
- `404 Not Found`: المخزن المصدر غير موجود
- `404 Not Found`: المخزن الهدف غير موجود
- `404 Not Found`: المنتج غير موجود
- `400 Bad Request`: الكمية غير كافية في المخزن المصدر

---

#### DELETE `/warehouses/:id`
حذف مخزن

**الصلاحيات المطلوبة**: `branches.manage`

**Response** (200 OK):
```json
{
  "message": "تم حذف المخزن بنجاح"
}
```

**الأخطاء المحتملة**:
- `404 Not Found`: المخزن غير موجود
- `400 Bad Request`: لا يمكن حذف المخزن لأنه مرتبط ببيانات أخرى

---

## DTOs (Data Transfer Objects)

### CreateWarehouseDto
```typescript
{
  name: string;          // اسم المخزن (مطلوب، حد أقصى 255 حرف)
  code: string;          // كود المخزن (مطلوب، فريد، حد أقصى 50 حرف)
  address?: string;      // العنوان (اختياري)
  phone?: string;        // رقم الهاتف (اختياري، حد أقصى 50 حرف)
  email?: string;        // البريد الإلكتروني (اختياري، صيغة صحيحة)
  branchId: string;      // معرف الفرع (مطلوب، UUID)
  managerId?: string;    // معرف المدير (اختياري، UUID)
  isActive?: boolean;    // حالة النشاط (افتراضي: true)
}
```

**قواعد التحقق**:
- `name`: مطلوب، نص، حد أقصى 255 حرف
- `code`: مطلوب، نص، فريد، حد أقصى 50 حرف
- `address`: اختياري، نص
- `phone`: اختياري، نص، حد أقصى 50 حرف
- `email`: اختياري، بريد إلكتروني صحيح
- `branchId`: مطلوب، UUID صحيح
- `managerId`: اختياري، UUID صحيح
- `isActive`: اختياري، قيمة منطقية

---

### UpdateWarehouseDto
```typescript
{
  name?: string;         // اسم المخزن
  address?: string;      // العنوان
  phone?: string;        // رقم الهاتف
  email?: string;        // البريد الإلكتروني
  managerId?: string;    // معرف المدير
  isActive?: boolean;    // حالة النشاط
}
```

**قواعد التحقق**:
- جميع الحقول اختيارية
- `email`: إذا تم توفيره، يجب أن يكون بريد إلكتروني صحيح
- `managerId`: إذا تم توفيره، يجب أن يكون UUID صحيح

---

### TransferStockDto
```typescript
{
  fromWarehouseId: string;    // معرف المخزن المصدر (مطلوب، UUID)
  toWarehouseId: string;      // معرف المخزن الهدف (مطلوب، UUID)
  productVariantId: string;  // معرف متغير المنتج (مطلوب، UUID)
  quantity: number;           // الكمية (مطلوب، رقم موجب)
  notes?: string;            // ملاحظات (اختياري)
}
```

**قواعد التحقق**:
- `fromWarehouseId`: مطلوب، UUID صحيح
- `toWarehouseId`: مطلوب، UUID صحيح
- `productVariantId`: مطلوب، UUID صحيح
- `quantity`: مطلوب، رقم موجب
- `notes`: اختياري، نص

---

## الخدمات (Services)

### WarehouseService

#### `create(createWarehouseDto: CreateWarehouseDto): Promise<WarehouseWithDetails>`
إنشاء مخزن جديد

**المعاملات**:
- `createWarehouseDto`: بيانات إنشاء المخزن

**القيمة المرجعة**: المخزن المُنشأ مع التفاصيل

**الاستثناءات**:
- `ConflictException`: إذا كان كود المخزن موجوداً
- `NotFoundException`: إذا كان الفرع أو المدير غير موجود

---

#### `findAll(branchId?: string): Promise<WarehouseWithDetails[]>`
الحصول على جميع المخازن مع الفلترة

**المعاملات**:
- `branchId`: معرف الفرع (اختياري)

**القيمة المرجعة**: مصفوفة من المخازن

---

#### `findOne(id: string): Promise<WarehouseWithDetails>`
الحصول على مخزن بالمعرف

**المعاملات**:
- `id`: معرف المخزن

**القيمة المرجعة**: المخزن

**الاستثناءات**:
- `NotFoundException`: إذا كان المخزن غير موجود

---

#### `update(id: string, updateWarehouseDto: UpdateWarehouseDto): Promise<WarehouseWithDetails>`
تحديث مخزن

**المعاملات**:
- `id`: معرف المخزن
- `updateWarehouseDto`: بيانات التحديث

**القيمة المرجعة**: المخزن المحدث

**الاستثناءات**:
- `NotFoundException`: إذا كان المخزن أو المدير غير موجود

---

#### `remove(id: string): Promise<void>`
حذف مخزن

**المعاملات**:
- `id`: معرف المخزن

**الاستثناءات**:
- `NotFoundException`: إذا كان المخزن غير موجود
- `BadRequestException`: إذا كان المخزن مرتبط ببيانات أخرى

---

#### `getStockItemsByWarehouse(id: string): Promise<StockItem[]>`
الحصول على عناصر المخزون بالمخزن

**المعاملات**:
- `id`: معرف المخزن

**القيمة المرجعة**: مصفوفة من عناصر المخزون

---

#### `transferStock(fromWarehouseId: string, toWarehouseId: string, productVariantId: string, quantity: number, notes?: string): Promise<TransferResult>`
نقل المخزون بين المخازن

**المعاملات**:
- `fromWarehouseId`: معرف المخزن المصدر
- `toWarehouseId`: معرف المخزن الهدف
- `productVariantId`: معرف متغير المنتج
- `quantity`: الكمية
- `notes`: ملاحظات (اختياري)

**القيمة المرجعة**: نتيجة النقل

**الاستثناءات**:
- `NotFoundException`: إذا كان المخزن أو المنتج غير موجود
- `BadRequestException`: إذا كانت الكمية غير كافية

---

#### `getWarehouseStats(): Promise<WarehouseStats>`
الحصول على إحصائيات المخازن

**القيمة المرجعة**: إحصائيات المخازن

---

## العلاقات (Relationships)

### الوحدات المرتبطة

- **Branch Module**: المخازن تنتمي إلى فروع
- **User Module**: المخازن لديها مديرون
- **Inventory Module**: المخازن تحتوي على عناصر المخزون
- **Sales Module**: المخازن مرتبطة بفواتير المبيعات
- **Purchasing Module**: المخازن مرتبطة بأوامر وفواتير المشتريات
- **Audit Module**: جميع عمليات المخازن تُسجل في سجلات التدقيق

### التبعيات

- `PrismaService`: للوصول إلى جداول `warehouses`, `branches`, `users`, `stock_items`
- `CacheService`: لتخزين البيانات في الكاش
- `BranchModule`: للتحقق من صحة الفروع
- `UserModule`: للتحقق من صحة المستخدمين (المديرين)
- `InventoryModule`: لإدارة عناصر المخزون

### الجداول المرتبطة

- `warehouses`: جدول المخازن الرئيسي
- `branches`: جدول الفروع
- `users`: جدول المستخدمين
- `stock_items`: عناصر المخزون
- `stock_movements`: حركات المخزون
- `sales_invoices`: فواتير المبيعات
- `purchase_orders`: أوامر الشراء
- `audit_logs`: سجلات التدقيق

---

## أمثلة الاستخدام

### مثال 1: إنشاء مخزن جديد

```typescript
// Request
POST /warehouses
Authorization: Bearer <access_token>
{
  "name": "مخزن الفرع الثاني",
  "code": "WH002",
  "address": "عدن، شارع الجمهورية",
  "phone": "+967712345679",
  "email": "warehouse2@example.com",
  "branchId": "clx7777777777",
  "managerId": "clx2222222222",
  "isActive": true
}

// Response
{
  "id": "clx1212121212",
  "name": "مخزن الفرع الثاني",
  "code": "WH002",
  "branch": {
    "id": "clx7777777777",
    "name": "الفرع الثاني"
  },
  "stockItemCount": 0,
  "createdAt": "2025-12-01T10:00:00.000Z"
}
```

### مثال 2: نقل المخزون بين المخازن

```typescript
// Request
POST /warehouses/transfer-stock
Authorization: Bearer <access_token>
{
  "fromWarehouseId": "clx8888888888",
  "toWarehouseId": "clx1212121212",
  "productVariantId": "clx1010101010",
  "quantity": 50.5,
  "notes": "نقل مخزون للفرع الثاني"
}

// Response
{
  "message": "تم نقل المخزون بنجاح",
  "transfer": {
    "id": "clx1313131313",
    "fromWarehouseId": "clx8888888888",
    "toWarehouseId": "clx1212121212",
    "productVariantId": "clx1010101010",
    "quantity": 50.5,
    "createdAt": "2025-12-01T11:00:00.000Z"
  }
}
```

### مثال 3: الحصول على عناصر المخزون بالمخزن

```typescript
// Request
GET /warehouses/clx8888888888/stock
Authorization: Bearer <access_token>

// Response
[
  {
    "id": "clx9999999999",
    "quantity": 100.5,
    "minStock": 10,
    "maxStock": 1000,
    "productVariant": {
      "name": "منتج - لون أحمر",
      "product": {
        "name": "منتج"
      }
    }
  }
]
```

---

## الأخطاء الشائعة

### 409 Conflict - كود المخزن موجود بالفعل
**السبب**: محاولة إنشاء مخزن بكود موجود

**الحل**: 
- استخدام كود مخزن آخر
- أو تحديث المخزن الموجود

---

### 404 Not Found - الفرع غير موجود
**السبب**: معرف الفرع المرسل غير موجود في قاعدة البيانات

**الحل**: 
- التحقق من صحة معرف الفرع
- إنشاء الفرع أولاً إذا لم يكن موجوداً

---

### 400 Bad Request - الكمية غير كافية
**السبب**: محاولة نقل كمية أكبر من المتوفرة في المخزن المصدر

**الحل**: 
- التحقق من الكمية المتوفرة في المخزن المصدر
- نقل كمية أقل أو إضافة مخزون أولاً

---

### 400 Bad Request - لا يمكن حذف المخزن
**السبب**: المخزن مرتبط ببيانات أخرى (عناصر مخزون، فواتير، إلخ)

**الحل**: 
- إلغاء تفعيل المخزن بدلاً من حذفه
- أو حذف البيانات المرتبطة أولاً

---

**📅 تاريخ آخر تحديث**: ديسمبر 2025
**👨‍💻 المطور**: فريق تطوير BThwani
**📊 إصدار الوحدة**: v1.0.0

