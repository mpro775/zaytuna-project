# 📊 وحدة المخزون (Inventory Module)

## نظرة عامة

وحدة المخزون مسؤولة عن إدارة عناصر المخزون وحركاته في النظام. توفر هذه الوحدة وظائف شاملة لتتبع المخزون، تعديل الكميات، وإدارة تنبيهات المخزون المنخفض.

### الميزات الرئيسية

- **إدارة عناصر المخزون**: إنشاء وتحديث عناصر المخزون
- **تعديل المخزون**: تعديل كميات المخزون مع تسجيل الأسباب
- **حركات المخزون**: تتبع جميع حركات المخزون
- **تنبيهات**: تنبيهات المخزون المنخفض
- **الإحصائيات**: إحصائيات شاملة عن المخزون

### الاعتماديات

- `PrismaService`: للوصول إلى قاعدة البيانات
- `WarehouseModule`: للتحقق من صحة المخازن
- `ProductVariantModule`: للتحقق من صحة متغيرات المنتجات

---

## API Endpoints

### POST `/inventory/stock-items`
إنشاء عنصر مخزون جديد - **الصلاحيات**: `inventory.create`

### GET `/inventory/stock-items`
الحصول على عناصر المخزون - **الصلاحيات**: `inventory.read`
- Query: `warehouseId`, `lowStockOnly`

### GET `/inventory/stock-items/:id`
الحصول على عنصر مخزون بالمعرف - **الصلاحيات**: `inventory.read`

### PATCH `/inventory/stock-items/:id`
تحديث عنصر مخزون - **الصلاحيات**: `inventory.update`

### POST `/inventory/stock-items/:warehouseId/:productVariantId/adjust`
تعديل كمية المخزون - **الصلاحيات**: `inventory.update`

### GET `/inventory/movements`
الحصول على حركات المخزون - **الصلاحيات**: `inventory.read`
- Query: `warehouseId`, `productVariantId`, `limit`

### GET `/inventory/alerts/low-stock`
الحصول على تنبيهات المخزون المنخفض - **الصلاحيات**: `inventory.read`

### GET `/inventory/stats`
الحصول على إحصائيات المخزون - **الصلاحيات**: `inventory.read`

### GET `/inventory/products/:productVariantId/stock`
الحصول على مخزون منتج في جميع المخازن - **الصلاحيات**: `inventory.read`

### GET `/inventory/warehouses/:warehouseId/stock`
الحصول على مخزون مخزن محدد - **الصلاحيات**: `inventory.read`

---

## DTOs

### CreateStockItemDto
- `warehouseId`: string (مطلوب، UUID)
- `productVariantId`: string (مطلوب، UUID)
- `quantity`: number (افتراضي: 0)
- `minStock`: number (افتراضي: 0)
- `maxStock`: number (افتراضي: 1000)

### AdjustStockDto
- `quantity`: number (مطلوب)
- `movementType`: string (اختياري)
- `referenceType`: string (اختياري)
- `referenceId`: string (اختياري، UUID)
- `reason`: string (اختياري)

---

## العلاقات

- **Warehouse Module**: عناصر المخزون مرتبطة بالمخازن
- **Product Variant Module**: عناصر المخزون مرتبطة بمتغيرات المنتجات
- **Sales Module**: حركات المخزون تُسجل عند البيع
- **Purchasing Module**: حركات المخزون تُسجل عند الشراء

---

**📅 تاريخ آخر تحديث**: ديسمبر 2025

