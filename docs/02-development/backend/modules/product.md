# 📦 وحدة المنتجات (Product Module)

## نظرة عامة

وحدة المنتجات مسؤولة عن إدارة المنتجات الأساسية في النظام. توفر هذه الوحدة وظائف شاملة لإنشاء وتحديث وحذف المنتجات، بالإضافة إلى البحث والفلترة حسب الفئة أو النص.

### الميزات الرئيسية

- **إدارة المنتجات**: إنشاء وتحديث وحذف المنتجات
- **البحث**: البحث بالباركود أو SKU
- **الفلترة**: البحث حسب الفئة أو النص
- **الإحصائيات**: إحصائيات شاملة عن المنتجات

### الاعتماديات

- `PrismaService`: للوصول إلى قاعدة البيانات
- `CacheService`: لتخزين البيانات في الكاش
- `CategoryModule`: للتحقق من صحة الفئات

---

## API Endpoints

### POST `/products`
إنشاء منتج جديد - **الصلاحيات**: `products.create`

### GET `/products`
الحصول على جميع المنتجات - **الصلاحيات**: `products.read`
- Query: `categoryId`, `search`

### GET `/products/stats`
الحصول على إحصائيات المنتجات - **الصلاحيات**: `products.read`

### GET `/products/lookup/:identifier`
البحث عن منتج بالباركود أو SKU - **الصلاحيات**: `products.read`

### GET `/products/:id`
الحصول على منتج بالمعرف - **الصلاحيات**: `products.read`

### PATCH `/products/:id`
تحديث منتج - **الصلاحيات**: `products.update`

### DELETE `/products/:id`
حذف منتج - **الصلاحيات**: `products.delete`

---

## DTOs

### CreateProductDto
- `name`: string (مطلوب)
- `description`: string (اختياري)
- `barcode`: string (اختياري، فريد)
- `sku`: string (اختياري، فريد)
- `categoryId`: string (مطلوب، UUID)
- `basePrice`: number (مطلوب، ≥ 0)
- `costPrice`: number (اختياري، ≥ 0)
- `taxId`: string (اختياري، UUID)
- `trackInventory`: boolean (افتراضي: true)
- `reorderPoint`: number (اختياري، ≥ 0)
- `imageUrl`: string (اختياري)
- `isActive`: boolean (افتراضي: true)

---

## العلاقات

- **Category Module**: المنتجات تنتمي إلى فئات
- **Product Variant Module**: المنتجات لديها متغيرات
- **Inventory Module**: المنتجات مرتبطة بعناصر المخزون

---

**📅 تاريخ آخر تحديث**: ديسمبر 2025

