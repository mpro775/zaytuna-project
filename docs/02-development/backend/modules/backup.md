# 💾 وحدة النسخ الاحتياطي (Backup Module)

## نظرة عامة

وحدة النسخ الاحتياطي مسؤولة عن إنشاء واستعادة النسخ الاحتياطية لقاعدة البيانات. توفر هذه الوحدة نظام نسخ احتياطي شامل مع تشفير وضغط.

### الميزات الرئيسية

- **نسخ احتياطي يدوي**: إنشاء نسخ احتياطية عند الطلب
- **نسخ احتياطي تلقائي**: نسخ احتياطي مجدول
- **الاستعادة**: استعادة من نسخة احتياطية
- **التشفير**: تشفير النسخ الاحتياطية

---

## API Endpoints

#### POST `/backup/manual` - إنشاء نسخة احتياطية يدوية
**الصلاحيات**: `backup:create`

#### POST `/backup/restore` - استعادة نسخة احتياطية
**الصلاحيات**: `backup:restore`

#### GET `/backup/list` - قائمة النسخ الاحتياطية
**الصلاحيات**: `backup:read`

#### GET `/backup/stats` - إحصائيات النسخ الاحتياطي
**الصلاحيات**: `backup:read`

#### GET `/backup/:backupId` - تفاصيل نسخة احتياطية
**الصلاحيات**: `backup:read`

#### DELETE `/backup/:backupId` - حذف نسخة احتياطية
**الصلاحيات**: `backup:delete`

#### POST `/backup/automatic/:reason` - نسخة احتياطية تلقائية
**الصلاحيات**: `backup:create`

---

## العلاقات

- **Database**: النسخ الاحتياطي لقاعدة البيانات
- **User Module**: تتبع منشئي النسخ الاحتياطية
- **Branch Module**: النسخ الاحتياطي حسب الفروع

---

**📅 تاريخ آخر تحديث**: ديسمبر 2025

