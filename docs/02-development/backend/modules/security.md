# 🔒 وحدة الأمان (Security Module)

## نظرة عامة

وحدة الأمان مسؤولة عن إدارة إعدادات الأمان في النظام. توفر هذه الوحدة تقارير أمان والتحقق من إعدادات الأمان.

### الميزات الرئيسية

- **تقرير الأمان**: تقرير شامل عن حالة الأمان
- **التحقق من الإعدادات**: التحقق من صحة إعدادات الأمان
- **إعدادات CORS**: إدارة إعدادات CORS
- **Rate Limiting**: إدارة Rate Limiting

---

## API Endpoints

#### GET `/security/report` - تقرير الأمان
**الصلاحيات**: `security:read`

#### POST `/security/validate` - التحقق من الإعدادات
**الصلاحيات**: `security:admin`

#### GET `/security/cors` - إعدادات CORS
**الصلاحيات**: `security:read`

#### GET `/security/rate-limit` - إعدادات Rate Limiting
**الصلاحيات**: `security:read`

---

## العلاقات

- **جميع الوحدات**: جميع الوحدات تستخدم Security Module
- **Auth Module**: تكامل مع نظام المصادقة

---

**📅 تاريخ آخر تحديث**: ديسمبر 2025

