# 💳 وحدة المدفوعات (Payment Module)

## نظرة عامة

وحدة المدفوعات مسؤولة عن معالجة المدفوعات من خلال بوابات دفع متعددة (Stripe, PayPal, Tap, إلخ). توفر هذه الوحدة نظام دفع شامل مع دعم الاسترداد والتسوية.

### الميزات الرئيسية

- **معالجة المدفوعات**: معالجة المدفوعات من خلال بوابات متعددة
- **الاسترداد**: معالجة استرداد المدفوعات
- **روابط الدفع**: إنشاء روابط دفع و QR codes
- **التسوية**: تسوية المعاملات مع البوابات
- **الإحصائيات**: إحصائيات شاملة عن المدفوعات

---

## API Endpoints

### POST `/payment/process` - معالجة دفعة
**الصلاحيات**: `payment.create`

### POST `/payment/refund` - معالجة استرداد
**الصلاحيات**: `payment.refund`

### GET `/payment/transaction/:transactionId` - تفاصيل معاملة
**الصلاحيات**: `payment.read`

### GET `/payment/transactions` - معاملات المستخدم
**الصلاحيات**: `payment.read`
- Query: `page`, `limit`, `status`

### GET `/payment/stats` - إحصائيات الدفع
**الصلاحيات**: `payment.read`
- Query: `branchId`, `startDate`, `endDate`

### GET `/payment/gateways` - البوابات المتاحة
**الصلاحيات**: `payment.read`

### GET `/payment/gateways/:gateway` - معلومات بوابة
**الصلاحيات**: `payment.read`

### POST `/payment/create-link` - إنشاء رابط دفع
**الصلاحيات**: `payment.create`

### POST `/payment/create-qr` - إنشاء QR code
**الصلاحيات**: `payment.create`

### POST `/payment/reconcile` - تسوية المعاملات
**الصلاحيات**: `payment.admin`

### GET `/payment/reports/transactions` - تقرير المعاملات
**الصلاحيات**: `payment.read`

### GET `/payment/performance` - إحصائيات الأداء
**الصلاحيات**: `payment.read`

---

## DTOs

### PaymentRequest
- `invoiceId`: string (مطلوب)
- `invoiceType`: string (مطلوب)
- `gateway`: PaymentGateway (مطلوب)
- `method`: string (مطلوب)
- `amount`: number (مطلوب)
- `currency`: string (مطلوب)
- `customerId`: string (اختياري)
- `metadata`: object (اختياري)

### RefundRequest
- `transactionId`: string (مطلوب)
- `amount`: number (مطلوب)
- `reason`: string (اختياري)

---

## العلاقات

- **Sales Module**: المدفوعات مرتبطة بفواتير المبيعات
- **Customer Module**: المدفوعات مرتبطة بالعملاء
- **Branch Module**: المدفوعات مرتبطة بالفروع

---

**📅 تاريخ آخر تحديث**: ديسمبر 2025

