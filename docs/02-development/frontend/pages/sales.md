# صفحة إدارة المبيعات (Sales)

## نظرة عامة

صفحة إدارة المبيعات تسمح للمستخدمين بإدارة فواتير المبيعات، المدفوعات، والمرتجعات.

**الموقع**: `src/pages/Sales/SalesInvoicesList.tsx`  
**المسار**: `/sales`  
**الحالة**: 🔄 قيد التنفيذ (60%)

---

## الوظائف الرئيسية

### 1. عرض الفواتير
- قائمة فواتير المبيعات
- فلترة حسب التاريخ، العميل، الحالة
- ترقيم الصفحات
- البحث

### 2. إدارة الفواتير
- إنشاء فاتورة جديدة
- تعديل فاتورة (مسودة)
- عرض تفاصيل الفاتورة
- إلغاء فاتورة
- طباعة فاتورة

### 3. إدارة المدفوعات
- إضافة دفعة جديدة
- عرض تاريخ المدفوعات
- حساب المبلغ المتبقي

### 4. المرتجعات
- إنشاء إرجاع
- عرض المرتجعات
- معالجة المرتجعات

---

## المكونات المستخدمة

- `Table`: جدول الفواتير
- `Modal`: نوافذ الحوار
- `Button`: أزرار الإجراءات

---

## تدفق المستخدم (User Flow)

### تدفق إنشاء فاتورة
1. المستخدم ينقر "فاتورة جديدة"
2. يختار العميل
3. يضيف المنتجات
4. يحدد طريقة الدفع
5. يحفظ و/أو يطبع

---

## APIs المستخدمة

- `GET /sales/invoices` - جلب الفواتير
- `POST /sales/invoices` - إنشاء فاتورة
- `GET /sales/invoices/:id` - تفاصيل الفاتورة
- `POST /sales/invoices/:id/payments` - إضافة دفعة
- `GET /sales/invoices/:id/print` - طباعة الفاتورة

---

## حالات الاستخدام

### حالة الاستخدام 1: إنشاء فاتورة
المستخدم يريد إنشاء فاتورة بيع:
1. يفتح صفحة Sales
2. ينقر "فاتورة جديدة"
3. يختار العميل
4. يضيف المنتجات
5. يحفظ

---

## ملاحظات التطوير

- الفواتير يمكن حفظها كمسودة
- المدفوعات يمكن إضافتها لاحقاً
- الطباعة تستدعي API منفصل

---

**تاريخ آخر تحديث**: ديسمبر 2025

